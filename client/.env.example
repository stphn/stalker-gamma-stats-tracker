# Local dev — default is ws://localhost:3001, no need to set this
# VITE_WS_URL=ws://localhost:3001

# Vercel / remote — set to your Cloudflare Tunnel WSS URL
# VITE_WS_URL=wss://your-tunnel-name.trycloudflare.com
